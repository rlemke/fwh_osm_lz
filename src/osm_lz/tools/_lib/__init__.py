"""Workflow catalog metadata for osm-lz.

The catalog is the only Python "library" this package ships — it lets
the CLIs under ``tools/`` describe and submit workflows without parsing
FFL or hard-coding paths.
"""
